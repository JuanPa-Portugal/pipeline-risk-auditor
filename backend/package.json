{
  "name": "pipeline-risk-auditor-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest --run"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.600.0",
    "esbuild": "^0.28.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "typescript": "~5.7.0",
    "vitest": "^3.0.0"
  }
}
